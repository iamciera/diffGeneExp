```{r}
d <- d[rowSums(cpm.d>2)>=3,]
d <- d[rowSums(cpm.d>2)>=3,]
counts <- read.delim("../sam2countsResults.tsv",check.names=FALSE,row.names=1)
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
```{r, results = 'hide'}
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
#check the file
head(counts)
summary(counts)
head(counts)
counts[is.na(counts)] <- 0
wtcregion <- counts[,40:48]
head(wtcregion)
```
```{r}
#convert data to a form that edgeR wants
group <- c(rep("wtcmbr", 6), rep("wtcother",3))
d <- DGEList(counts=wtcregion,group=group)
d$samples
cpm.d<- cpm(d)
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,]
WT
----
Marginal Blastozone A vs Other, A region (tip) attempt 1
=========================================================
```{r}
library(edgeR)
```
###Read in Data
Read in raw count data per gene.
Add checknames to FALSE because it was making the columns unique.
```{r, results='hide'}
counts <- read.delim("../sam2countsResults.tsv",check.names=FALSE,row.names=1)
#check the file
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
```
###Subset per DE expirement
I am going to start by subsetting the particular treatments I am looking at.
```{r}
colnames(counts)
wtaregion <- counts[,c(24:26, 27:31)]
head(wtaregion)
colnames(wtaregion)
```
```{r}
group <- c(rep("wtambr", 3), rep("wtaother", 5))
d <- DGEList(counts=wtaregion,group=group)
d$samples
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,]
d <- d[rowSums(cpm.d>5)>=3,]
d <- d[rowSums(cpm.d>2)>=3,]
cpm.d<- cpm(d)
group <- c(rep("wtambr", 3), rep("wtaother", 5))
d <- DGEList(counts=wtaregion,group=group)
d$samples
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtaother"))
head(DEtest$table)
head(DEtest$table)
sum(DEtest$table$PValue<.01)
summary(decideTestsDGE(DEtest,p.value=.01))
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtaother"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
group <- c(rep("wtambr", 3), rep("wtaother", 5))
d <- DGEList(counts=wtaregion,group=group)
d$samples
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>6)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtaother"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
results.sig <- subset(DEtest$table, DEtest$table$PValue < 0.05)
group <- c(rep("wtambr", 3), rep("wtaother", 5))
d <- DGEList(counts=wtaregion,group=group)
d$samples
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtaother"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
results.sig <- subset(DEtest$table, DEtest$table$PValue < 0.05)
annotation1<- read.delim("ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
```{r, results='hide'}
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
```
Write table with results
write.table(results.annotated,"wtambr_wtaother.txt",sep="\t",row.names=F)
write.table(results.annotated,"wtambr_wtaother.txt",sep="\t",row.names=F)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
write.table(results.annotated,"wtambr_wtaother.txt",sep="\t", row.names=F)
render("wtambr_wtaother.Rmd", "pdf_document")
render("wtambr_wtaother.Rmd", "pdf_document")
setwd("~/Desktop/mdsedger_dede_annotation")
setwd("~/Desktop/mdsedger_dede_annotation")
render("wtambr_wtaother.Rmd", "pdf_document")
setwd("~/Desktop/mdsedger_dede_annotation")
setwd("~/Desktop/mdsedger_dede_annotation/wtambr_wtaother")
render("wtambr_wtaother.Rmd", "pdf_document")
render("wtambr_wtaother.Rmd", "pdf_document")
WT vs *tf2*
----
*tf2* Marginal Blastozone C (base) vs Marginal Blastzone in C (base) region
====================================================
```{r}
library(edgeR)
```
###Read in Data
Read in raw count data per gene.
```{r, results='hide'}
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
#check the file
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
```
###Subset per DE expirement
I am going to start by subsetting the particular treatments I am looking at.
```{r}
colnames(counts)
tf2cmbrVSwtcmbr <- counts[,c(16:19,42:45)]
head(tf2cmbrVSwtcmbr)
colnames(tf2cmbrVSwtcmbr)
```
```{r}
group <- c(rep("tf2cmbr", 4), rep("wtcmbr", 4))
d <- DGEList(counts=tf2cmbrVSwtcmbr,group=group)
```
```{r}
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,] #change to 5
<- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("tf2cmbr","wtcmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
WT vs *tf2*
----
*tf2* Marginal Blastozone C (base) vs Marginal Blastzone in C (base) region
====================================================
```{r}
library(edgeR)
```
###Read in Data
Read in raw count data per gene.
```{r, results='hide'}
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
#check the file
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
```
###Subset per DE expirement
I am going to start by subsetting the particular treatments I am looking at.
```{r}
colnames(counts)
tf2cmbrVSwtcmbr <- counts[,c(16:19,42:45)]
head(tf2cmbrVSwtcmbr)
colnames(tf2cmbrVSwtcmbr)
```
```{r}
group <- c(rep("tf2cmbr", 4), rep("wtcmbr", 4))
d <- DGEList(counts=tf2cmbrVSwtcmbr,group=group)
```
```{r}
d$samples
```
```{r}
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,] #change to 5
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("tf2cmbr","wtcmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
results.sig <- subset(DEtest$table, DEtest$table$PValue < 0.05)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation, results='hide')
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
write.table(results.annotated,"tf2cmbr_wtcmbr_DE1.txt",sep="\t",row.names=F)
setwd("~/Desktop/mdsedger_dede_annotation/tf2cmbr_wtcmbr")
render("tf2cmbr_wtcmbr.Rmd", "pdf_document")
```{r, results='hide'}
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
colnames(counts)
WTambrVSbmbr <- counts[,c(24:26,32:34)]
head(WTambrVSbmbr)
colnames(WTambrVSbmbr)
group <- c(rep("wtambr", 3), rep("wtbmbr", 3))
d <- DGEList(counts=WTambrVSbmbr,group=group)
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtbmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
d <- d[rowSums(cpm.d>5)>=3,]
colnames(counts)
WTambrVSbmbr <- counts[,c(24:26,32:34)]
head(WTambrVSbmbr)
colnames(WTambrVSbmbr)
```{r}
group <- c(rep("wtambr", 3), rep("wtbmbr", 3))
d <- DGEList(counts=WTambrVSbmbr,group=group)
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtbmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
results.sig <- subset(DEtest$table, DEtest$table$PValue < 0.05)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
setwd("~/Desktop/mdsedger_dede_annotation/wtambr_wtbmbr")
write.table(results.annotated,"wtambr_wtbmbr_DE1.txt",sep="\t",row.names=F)
write.table(results.annotated,"wtambr_wtbmbr_DE1.txt",sep="\t",row.names=F)
render("wtambr_wtbmbr.Rmd", "pdf_document")
setwd("~/Desktop/mdsedger_dede_annotation/wtambr_wtcmbr")
library(edgeR)
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
head(counts)
summary(counts)
colnames(counts)
counts[is.na(counts)] <- 0
colnames(counts)
colnames(counts)
WTambrVScmbr <- counts[,c(32:34,42:45)]
head(WTambrVScmbr)
colnames(WTambrVScmbr)
colnames(counts)
colnames(counts)
WTambrVScmbr <- counts[,c(24:26,42:45)]
head(WTambrVScmbr)
colnames(WTambrVScmbr)
group <- c(rep("wtambr", 3), rep("wtcmbr", 4))
d <- DGEList(counts=WTambrVScmbr,group=group)
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtbmbr","wtcmbr"))
head(DEtest$table)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtcmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.01)
summary(decideTestsDGE(DEtest,p.value=.01))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.01,])
plotSmear(d,de.tags=sig.genes)
d$samples
group <- c(rep("wtambr", 3), rep("wtcmbr", 4))
d <- DGEList(counts=WTambrVScmbr,group=group)
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtcmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.01)
summary(decideTestsDGE(DEtest,p.value=.01))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.01,])
plotSmear(d,de.tags=sig.genes)
group <- c(rep("wtambr", 3), rep("wtcmbr", 4))
d <- DGEList(counts=WTambrVScmbr,group=group)
```
There are two libraries that are super small, what is going on here? Should I drop them from analaysis?
```{r}
d$samples
```
```{r}
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtambr","wtcmbr"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
results.sig <- subset(DEtest$table, DEtest$table$PValue < 0.05)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
write.table(results.annotated,"wtambr_wtcmbr_DE1.tsv",sep="\t",row.names=F)
setwd("~/Desktop/mdsedger_dede_annotation/wtambr_wtcmbr")
setwd("~/Desktop/mdsedger_dede_annotation/wtambr_wtcmbr")
render("wtambr_wtcmbr.Rmd", "pdf_document")
write.table(results.annotated,"wtambr_wtcmbr_DE1.txv",sep="\t",row.names=F)
library(edgeR)
counts <- read.delim("../sam2countsResults.tsv",check.names=FALSE,row.names=1)
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
head(wtbregion)
wtbregion <- counts[,c(32:34, 37:39)]
head(wtbregion)
colnames(wtbregion)
group <- c(rep("wtbmbr", 3), rep("wtbother", 3))
d <- DGEList(counts=wtbregion,group=group)
```
There are two libraries that are super small, what is going on here? Should I drop them from analaysis?
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,]
d <- estimateCommonDisp(d,verbose=T) #No error this time.
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtbmbr","wtbother"))
head(DEtest$table)
sum(DEtest$table$PValue<.01)
summary(decideTestsDGE(DEtest,p.value=.01))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.01,])
plotSmear(d,de.tags=sig.genes)
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T) #No error this time.
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtbmbr","wtbother"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
counts <- read.delim("../sam2countsResults.tsv",check.names=FALSE,row.names=1)
setwd("~/Desktop/mdsedger_dede_annotation/wtbmbr_wtbother")
counts <- read.delim("../sam2countsResults.tsv",check.names=FALSE,row.names=1)
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
```
###Subset per DE expirement
colnames(counts)
wtbregion <- counts[,c(32:34, 37:39)]
head(wtbregion)
colnames(wtbregion)
```
```{r}
group <- c(rep("wtbmbr", 3), rep("wtbother", 3))
d <- DGEList(counts=wtbregion,group=group)
```
```{r}
d$samples
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T) #No error this time.
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtbmbr","wtbother"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
cpm.d <- cpm(d)
d <- d[rowSums(cpm.d>5)>=3,]
d <- estimateCommonDisp(d,verbose=T) #No error this time.
d <- calcNormFactors(d)
d <- estimateCommonDisp(d)
DEtest <- exactTest(d,pair=c("wtbmbr","wtbother"))
head(DEtest$table)
sum(DEtest$table$PValue<.05)
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
results.sig <- subset(DEtest$table, DEtest$table$PValue < 0.05)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
results.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
write.table(results.annotated,"wtbmbr_wtbother_DE1.attempt2.txt",sep="\t",row.names=F)
render("wtbmbr_wtbother.attempt2.Rmd", "pdf_document")
`source("http://bioconductor.org/biocLite.R")`
`biocLite("edgeR")`
```{r}
library(edgeR)
```
counts <- read.delim("../sam2countsResults.tsv",row.names=1)
#check the file
head(counts)
summary(counts)
colnames(counts)
#need to convert NA to 0 counts
counts[is.na(counts)] <- 0
```
wtcregion <- counts[,40:48]
head(wtcregion)
```
group <- c(rep("wtcmbr", 6), rep("wtcother",3))
d <- DGEList(counts=wtcregion,group=group)
d$samples
cpm.d<- cpm(d)
d <- d[rowSums(cpm.d>2)>=3,]
d <- estimateCommonDisp(d,verbose=T)
d <- calcNormFactors(d)
DEtest <- exactTest(d,pair=c("wtcmbr","wtcother"))
head(DEtest$table)
dim(DEtest$table)
sum(DEtest$table$PValue<.05)  #changed from sum(results$table$adj.P.Val<.01) which resulted in 0
summary(decideTestsDGE(DEtest,p.value=.05))
sig.genes <- rownames(DEtest$table[DEtest$table$PValue<0.05,])
plotSmear(d,de.tags=sig.genes)
annotation1<- read.delim("../ITAG2.3_all_Arabidopsis_ITAG_annotations.tsv", header=FALSE)  #Changed to the SGN human readable annotation
colnames(annotation1)<- c("ITAG", "SGN_annotation")
annotation2<- read.delim ("../ITAG2.3_all_Arabidopsis_annotated.tsv")
annotation <- merge (annotation1,annotation2, by =1,1, all.x=TRUE)
head(annotation)
DEtest.annotated <- merge(results.sig,annotation,by.x="row.names",by.y="ITAG",all.x=T,sort=F)
head(DEtest.annotated,n=30)
setwd("~/Desktop/mdsedger_dede_annotation/wtbmbr_wtbother")
render("wtcmbr_wtcother.Rmd", "pdf_document")
setwd("~/Desktop/mdsedger_dede_annotation/wtcmbr_wtcother")
render("wtcmbr_wtcother.Rmd", "pdf_document")
write.table(DEtest.annotated,"wtcmbr_wtcother_DE.txt",sep="\t",row.names=F)
